# Copy to .env and edit as needed

# SQLite database path (relative to project root)
MOTOR_DB_PATH=motor_data.db

# Server host/port
HOST=127.0.0.1
PORT=8000

# Set to "production" in production
ENV=development
x